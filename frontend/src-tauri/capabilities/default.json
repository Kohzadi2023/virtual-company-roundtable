{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main Virtual Company window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
